name: rx_event_channel
description: Allows communication between Flutter and platform-specific code using various types of RxDart Subjects
version: 1.0.1
repository: https://github.com/m7mdra/rx_event_channel


environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=2.5.0"

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.28.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
